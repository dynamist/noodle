apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
# Local overrides such as ODOO_MODULES=contacts,crm (gitignored, created empty by `make up`)
configMapGenerator:
  - name: odoo-config
    behavior: merge
    envs: [config.local.env]
